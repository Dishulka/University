#include <iostream>
using namespace std;



long double calcP0(long double maintenanceTime, long double appearanceTime, long double waitingTime, int n)
{
	long double EPS = 0e-4;

	long double sumDenum1 = 0;
	long double ro = maintenanceTime / appearanceTime;
	for (int i = 0; i <= n; ++i)
	{
		
	}

	
	

		//(1 / appearanceTime) / ((n / maintenanceTime) + (1 / waitingTime));
}


int main()
{
	long double maintenanceTime = 10;
	long double appearanceTime = 32; 
	long double waitingTime = 7;
	long double max = 3;



	return 0;
}